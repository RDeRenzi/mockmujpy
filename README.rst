.. encoding: utf-8

=============
Mock mudashed
=============

Barebone mudashed mujpy gui interfacei, based on ipywidgets, towards skimming a snippet that enucleates the cause of the windows python kernel crash under jupyter lab and notebook, while in linux it works fine. 

Works on linux, but does not help on win: all buttons are broken! It works even less than original mujpy.

Conclusion: forget mujpy on windows python. Explore WSL2 + minimal ubuntu (although users might not like to install ubuntu inside their windows)
